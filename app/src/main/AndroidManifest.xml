<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.greekwetlandkerkinitour">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/greek_wetland_kerkini"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActionsActivity"
            android:label="@string/category_actions" />
        <activity
            android:name=".HotelsActivity"
            android:label="@string/category_hotels" />
        <activity
            android:name=".FoodActivity"
            android:label="@string/category_food" />
        <activity
            android:name=".PhotosActivity"
            android:label="@string/category_photos" />
        <activity android:name=".GaleryAdapter" />
        <activity android:name=".Galery" />
        <activity android:name=".InfoActivity"></activity>
    </application>

</manifest>